name: Build FAP

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

jobs:
  # The grade is what the user actually walks away with, and it is the one part
  # a screenshot cannot vouch for, so every family code, boundary and string
  # bound is checked on each push. The engine is plain C with no Flipper headers
  # precisely so this job can exist.
  test:
    runs-on: ubuntu-latest
    name: grading engine
    steps:
      - uses: actions/checkout@v4
      - name: Run host tests
        run: make -C test

  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        channel: [release, dev]
    name: ufbt (${{ matrix.channel }})
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install ufbt
        run: python3 -m pip install --upgrade ufbt

      - name: Sync SDK (${{ matrix.channel }})
        run: ufbt update --channel=${{ matrix.channel }}

      - name: Build Talos
        run: ufbt

      - name: Upload .fap
        uses: actions/upload-artifact@v4
        with:
          name: talos-fap-${{ matrix.channel }}
          path: dist/*.fap
          if-no-files-found: error
